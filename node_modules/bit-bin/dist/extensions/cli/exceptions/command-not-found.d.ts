import { PaperError } from './paper-error';
export declare class CommandNotFound extends PaperError {
    commandName: string;
    suggestion?: string;
    constructor(commandName: string, suggestion?: string);
    report(): string;
}
