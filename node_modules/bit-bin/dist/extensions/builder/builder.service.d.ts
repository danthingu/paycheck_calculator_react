import { EnvService, ExecutionContext } from '../environments';
import { IsolatorExtension } from '../isolator';
import { Workspace } from '../workspace';
import { LogPublisher } from '../types';
export declare class BuilderService implements EnvService {
    private isolator;
    private workspace;
    private logger;
    constructor(isolator: IsolatorExtension, workspace: Workspace, logger: LogPublisher);
    run(context: ExecutionContext): Promise<{
        id: string;
        results: any;
    }>;
}
