import { BuildTask, BuildContext } from './types';
import { LogPublisher } from '../types';
export declare class BuildPipe {
    readonly tasks: BuildTask[];
    readonly logger: LogPublisher;
    constructor(tasks: BuildTask[], logger: LogPublisher);
    execute(buildContext: BuildContext): Promise<any>;
    static from(tasks: BuildTask[], logger: LogPublisher): BuildPipe;
}
