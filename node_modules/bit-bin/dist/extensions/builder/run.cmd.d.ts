import { Command, CommandOptions } from '../cli';
import { Workspace } from '../workspace';
import { BuilderExtension } from './builder.extension';
import { Reporter } from '../reporter';
export declare class BuilderCmd implements Command {
    private builder;
    private workspace;
    private reporter;
    name: string;
    description: string;
    alias: string;
    group: string;
    private: boolean;
    shortDescription: string;
    options: CommandOptions;
    constructor(builder: BuilderExtension, workspace: Workspace, reporter: Reporter);
    report([userPattern]: [string], { verbose }: {
        verbose: boolean;
    }): Promise<string>;
}
