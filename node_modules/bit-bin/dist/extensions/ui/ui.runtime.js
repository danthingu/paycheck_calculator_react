"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

function _harmony() {
  const data = _interopRequireDefault(require("@teambit/harmony"));

  _harmony = function () {
    return data;
  };

  return data;
}

function _ui() {
  const data = require("./ui.ui");

  _ui = function () {
    return data;
  };

  return data;
}

function _docs() {
  const data = require("../docs/docs.ui");

  _docs = function () {
    return data;
  };

  return data;
}

/**
 * configure all core extensions
 * :TODO pass all other extensions from above.
 */
_harmony().default.run([_ui().UIRuntimeExtension, _docs().DocsUI]).then(() => {
  const uiExtension = _harmony().default.get('UIRuntimeExtension');

  uiExtension.render();
}).catch(err => {
  throw err;
});