import React from 'react';
import { Command } from '../cli';
import { Workspace } from '../workspace';
import { UIExtension } from './ui.extension';
export declare class StartCmd implements Command {
    private ui;
    private workspace;
    name: string;
    description: string;
    alias: string;
    private: boolean;
    group: string;
    shortDescription: string;
    options: never[];
    constructor(ui: UIExtension, workspace: Workspace);
    private clearConsole;
    render([userPattern]: [string]): Promise<React.ReactElement>;
}
export declare function EnvConsole(props: any): JSX.Element;
