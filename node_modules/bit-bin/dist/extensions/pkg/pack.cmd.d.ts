/// <reference types="react" />
import { Packer, PackOptions } from './pack';
import { CommandOptions, Command } from '../cli';
declare type PackArgs = [string, string];
export declare class PackCmd implements Command {
    private packer;
    name: string;
    description: string;
    options: CommandOptions;
    shortDescription: string;
    alias: string;
    group: string;
    constructor(packer: Packer);
    render(args: PackArgs, options: PackOptions): Promise<JSX.Element>;
    json([componentId, scopePath]: PackArgs, options: PackOptions): Promise<{
        data: import("./pack").PackResult;
        code: number;
    }>;
}
export {};
