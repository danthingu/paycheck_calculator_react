import { Compiler } from '../compiler';
import { Network } from '../isolator';
import { BuildResults } from '../builder';
export declare class TypescriptCompiler implements Compiler {
    readonly tsConfig: Record<string, any>;
    constructor(tsConfig: Record<string, any>);
    compileFile(fileContent: string, options: {
        componentDir: string;
        filePath: string;
    }): {
        outputText: string;
        outputPath: string;
    }[] | null;
    compileOnCapsules({ capsuleGraph }: {
        capsuleGraph: Network;
    }): Promise<BuildResults>;
}
