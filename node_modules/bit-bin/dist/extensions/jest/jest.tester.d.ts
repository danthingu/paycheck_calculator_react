import { Tester, TestResults, TesterContext } from '../tester';
export declare class JestTester implements Tester {
    readonly jestConfig: any;
    constructor(jestConfig: any);
    test(context: TesterContext): Promise<TestResults>;
}
