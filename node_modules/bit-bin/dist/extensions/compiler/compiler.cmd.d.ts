/// <reference types="react" />
import { Command, CommandOptions } from '../cli';
import { Compile } from './compile';
export declare class CompileCmd implements Command {
    private compile;
    name: string;
    description: string;
    shortDescription: string;
    alias: string;
    group: string;
    private: boolean;
    options: CommandOptions;
    constructor(compile: Compile);
    render([components]: [string[]], { verbose, noCache }: {
        verbose: boolean;
        noCache: boolean;
    }): Promise<JSX.Element>;
    json([components]: [string[]], { verbose, noCache }: {
        verbose: boolean;
        noCache: boolean;
    }): Promise<{
        data: any;
        code: number;
    }>;
}
