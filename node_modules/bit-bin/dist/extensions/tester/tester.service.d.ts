import { EnvService, ExecutionContext } from '../environments';
import { TestResults } from './tester';
import { Workspace } from '../workspace';
export declare class TesterService implements EnvService {
    readonly workspace: Workspace;
    readonly testsRegex: string;
    constructor(workspace: Workspace, testsRegex: string);
    run(context: ExecutionContext): Promise<TestResults>;
}
