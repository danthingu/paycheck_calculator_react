import { Harmony } from '@teambit/harmony';
import { Workspace } from '../workspace';
import { ScopeExtension } from '../scope';
import { Config } from '../config';
import { Logger } from '../logger';
import Core from './core';
export declare type CoreDeps = [Config, Logger, Workspace, ScopeExtension];
export declare type CoreConfig = {};
export default function provideCore([config, logger, workspace, scope]: CoreDeps, _config: any, _slots: any, harmony: Harmony): Promise<Core>;
