import Config from './config';
import { Store } from './store';
import ComponentFS from './component-fs';
import ConsumerComponent from '../../consumer/component';
export default class State {
    readonly config: Config;
    readonly filesystem: ComponentFS;
    readonly store: Store;
    readonly dependencies: any;
    readonly _consumer: ConsumerComponent;
    constructor(config: Config, filesystem: ComponentFS, store: Store, dependencies: any, _consumer: ConsumerComponent);
    get hash(): string;
    dependencyGraph(): void;
    static fromLegacy(consumerComponent: ConsumerComponent): State;
}
