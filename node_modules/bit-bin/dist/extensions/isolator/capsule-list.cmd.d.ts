/// <reference types="react" />
import { Command, CommandOptions } from '../cli';
import { IsolatorExtension, ListResults } from './isolator.extension';
export declare class CapsuleListCmd implements Command {
    private isolator;
    name: string;
    description: string;
    shortDescription: string;
    group: string;
    private: boolean;
    alias: string;
    options: CommandOptions;
    constructor(isolator: IsolatorExtension);
    getList(): Promise<ListResults>;
    render(): Promise<JSX.Element>;
    json(): Promise<ListResults>;
}
