import Reporter from './reporter';
import { Logger } from '../logger';
export declare type ReporterDeps = [Logger];
export declare function provideReporter([logger]: ReporterDeps): Promise<Reporter>;
