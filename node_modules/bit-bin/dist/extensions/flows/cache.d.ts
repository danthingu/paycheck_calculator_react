import { Capsule } from '../isolator';
export declare class ExecutionCache {
    private pathToCache;
    constructor(pathToCache: string);
    hash(capsule: Capsule, name: string): string;
    saveHashValue(capsule: Capsule, name: string): Promise<void>;
    getCacheValue(wrkDir: string, name: string): Promise<string | undefined>;
    compareToCache(capsule: Capsule, name: string): Promise<boolean>;
}
export declare function getExecutionCache(): ExecutionCache;
