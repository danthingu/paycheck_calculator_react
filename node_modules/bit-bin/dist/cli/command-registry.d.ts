import { LegacyCommand } from './legacy-command';
import { Commands } from '../legacy-extensions/extension';
import { Command } from './command';
export declare function execAction(command: Command, concrete: any, args: any): Promise<any>;
export declare function register(command: Command, commanderCmd: any, packageManagerArgs?: string[]): void;
export default class CommandRegistry {
    version: string;
    usage: string;
    description: string;
    commands: LegacyCommand[];
    extensionsCommands: LegacyCommand[] | null | undefined;
    registerBaseCommand(): void;
    constructor(usage: string, description: string, version: string, commands: LegacyCommand[], extensionsCommands: Array<Commands>);
}
