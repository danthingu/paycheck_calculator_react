import { LegacyCommand, CommandOptions } from '../../legacy-command';
import SpecsResults from '../../../consumer/specs-results';
import Dists from '../../../consumer/component/sources/dists';
import { PathOsBased } from '../../../utils/path';
export default class CiUpdate implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    private: boolean;
    action([id, scopePath]: [string, string | null | undefined], { directory, output, keep, noCache }: {
        directory?: string;
        output?: string;
        keep: boolean;
        noCache: boolean;
    }): Promise<any>;
    report({ specsResults, dists, output, directory }: {
        specsResults: SpecsResults | undefined;
        dists: Dists;
        output: PathOsBased;
        directory: PathOsBased;
    }): string;
}
