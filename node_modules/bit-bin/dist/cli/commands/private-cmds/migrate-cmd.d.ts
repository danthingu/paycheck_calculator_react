import { LegacyCommand, CommandOptions } from '../../legacy-command';
export default class Migrate implements LegacyCommand {
    name: string;
    description: string;
    private: boolean;
    loader: boolean;
    migration: boolean;
    alias: string;
    opts: CommandOptions;
    action([scopePath]: [string], { verbose }: {
        verbose: boolean | null | undefined;
    }): Promise<any>;
    report(): string;
}
