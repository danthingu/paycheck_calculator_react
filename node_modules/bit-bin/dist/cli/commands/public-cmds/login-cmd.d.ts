import { LegacyCommand, CommandOptions } from '../../legacy-command';
export default class Login implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    skipWorkspace: boolean;
    opts: CommandOptions;
    action([]: Iterable<any>, { port, suppressBrowserLaunch, npmrcPath, skipRegistryConfig, machineName }: {
        port: string;
        suppressBrowserLaunch?: boolean;
        npmrcPath: string;
        skipRegistryConfig: boolean;
        machineName?: string;
    }): Promise<any>;
    report({ isAlreadyLoggedIn, username, npmrcPath, skipRegistryConfig, writeToNpmrcError }: {
        isAlreadyLoggedIn: boolean;
        username: string;
        npmrcPath: string;
        skipRegistryConfig: boolean;
        writeToNpmrcError: boolean;
    }): string;
}
