import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { TagResults } from '../../../api/consumer/lib/tag';
export declare const NOTHING_TO_TAG_MSG = "nothing to tag";
export declare const AUTO_TAGGED_MSG = "auto-tagged dependents";
export default class Tag implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    action([id, version]: string[], { message, all, patch, minor, major, force, verbose, ignoreMissingDependencies, ignoreUnresolvedDependencies, ignoreNewestVersion, skipTests, skipAutoTag, scope }: {
        message?: string;
        all?: boolean;
        patch?: boolean;
        minor?: boolean;
        major?: boolean;
        force?: boolean;
        verbose?: boolean;
        ignoreMissingDependencies?: boolean;
        ignoreUnresolvedDependencies?: boolean;
        ignoreNewestVersion?: boolean;
        skipTests?: boolean;
        skipAutoTag?: boolean;
        scope?: string;
    }): Promise<any>;
    report(results: TagResults): string;
}
