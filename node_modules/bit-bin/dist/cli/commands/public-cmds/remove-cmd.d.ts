import { LegacyCommand, CommandOptions } from '../../legacy-command';
import RemovedObjects from '../../../scope/removed-components';
import RemovedLocalObjects from '../../../scope/removed-local-objects';
export default class Remove implements LegacyCommand {
    name: string;
    description: string;
    skipWorkspace: boolean;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    action([ids]: [string], { force, remote, track, deleteFiles, silent }: {
        force: boolean;
        remote: boolean;
        track: boolean;
        deleteFiles: boolean;
        silent: boolean;
    }): Promise<any>;
    report({ localResult, remoteResult }: {
        localResult: RemovedLocalObjects;
        remoteResult: RemovedObjects[];
    }): string;
    paintArray(removedObjectsArray: RemovedObjects[]): any[];
}
