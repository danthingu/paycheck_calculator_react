import { LegacyCommand } from '../../legacy-command';
export default class Logout implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    action(): Promise<any>;
    report(): string;
}
