import { LegacyCommand, CommandOptions } from '../../legacy-command';
export default class Build implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    action([id]: string[], { noCache, verbose }: {
        noCache: boolean;
        verbose: boolean;
    }): Promise<any>;
    report(res: string[] | null | undefined | string | Record<string, any>): string;
}
