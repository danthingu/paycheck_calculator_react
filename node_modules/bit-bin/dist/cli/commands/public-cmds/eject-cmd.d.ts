import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { EjectResults } from '../../../consumer/component-ops/eject-components';
export default class Eject implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    action([ids]: [string[]], { force, json }: {
        force: boolean;
        json: boolean;
    }): Promise<EjectResults>;
    report({ ejectResults, json }: {
        ejectResults: EjectResults;
        json: boolean;
    }): string;
}
