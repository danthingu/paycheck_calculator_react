import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { ApplyVersionResults, ApplyVersionResult } from '../../../consumer/versions-ops/merge-version';
export declare const applyVersionReport: (components: ApplyVersionResult[], addName?: boolean, showVersion?: boolean) => string;
export default class Merge implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    action([version, ids]: [string, string[]], { ours, theirs, manual }: {
        ours?: boolean;
        theirs?: boolean;
        manual?: boolean;
    }): Promise<ApplyVersionResults>;
    report({ components, version }: ApplyVersionResults): string;
}
