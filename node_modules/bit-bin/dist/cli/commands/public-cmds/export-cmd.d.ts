import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { BitId } from '../../../bit-id';
import { EjectResults } from '../../../consumer/component-ops/eject-components';
export default class Export implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    action([remote, ids]: [string, string[]], { eject, includeDependencies, setCurrentScope, all, allVersions, force, rewire }: any): Promise<any>;
    report({ componentsIds, nonExistOnBitMap, missingScope, ejectResults, remote, includeDependencies }: {
        componentsIds: BitId[];
        nonExistOnBitMap: BitId[];
        missingScope: BitId[];
        ejectResults: EjectResults | null | undefined;
        remote: string;
        includeDependencies: boolean;
    }): string;
}
