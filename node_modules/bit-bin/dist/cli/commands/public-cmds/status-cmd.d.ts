import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { StatusResult } from '../../../api/consumer/lib/status';
export declare const statusFailureMsg = "issues found";
export declare const statusInvalidComponentsMsg = "invalid components";
export declare const statusWorkspaceIsCleanMsg = "nothing to tag or export (use \"bit add <file...>\" to track files or directories as components)";
export default class Status implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    json: boolean;
    action([]: Iterable<any>, { json }: {
        json?: boolean;
    }): Promise<Record<string, any>>;
    report({ newComponents, modifiedComponent, stagedComponents, componentsWithMissingDeps, importPendingComponents, autoTagPendingComponents, invalidComponents, outdatedComponents }: StatusResult): string;
}
