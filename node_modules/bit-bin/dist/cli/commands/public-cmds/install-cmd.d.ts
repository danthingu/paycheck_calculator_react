import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { LinksResult } from '../../../links/node-modules-linker';
export default class Install implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    action([ids]: [string[]], { verbose }: {
        verbose?: boolean;
    }, packageManagerArgs: string[]): Promise<LinksResult[]>;
    report(results: LinksResult[]): string;
}
