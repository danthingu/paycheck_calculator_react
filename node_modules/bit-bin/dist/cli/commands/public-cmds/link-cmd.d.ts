import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { LinksResult } from '../../../links/node-modules-linker';
import { CodemodResult } from '../../../consumer/component-ops/codemod-components';
export default class Link implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    private: boolean;
    loader: boolean;
    action([ids]: [string[]], { rewire }: {
        rewire: boolean;
    }): Promise<any>;
    report(results: {
        linksResults: LinksResult[];
        codemodResults?: CodemodResult[];
    }, _args: any, flags: Record<string, any>): string;
}
