import { LegacyCommand, CommandOptions } from '../../legacy-command';
export default class Log implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    migration: boolean;
    remoteOp: boolean;
    skipWorkspace: boolean;
    action([id]: [string], { remote }: {
        remote: boolean;
    }): Promise<any>;
    report(logs: Array<{
        message: string;
        tag: string;
        date: string;
        username: string | null | undefined;
        email: string | null | undefined;
    }>): string;
}
