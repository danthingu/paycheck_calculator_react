import { LegacyCommand, CommandOptions } from '../../legacy-command';
import { ApplyVersionResults } from '../../../consumer/versions-ops/merge-version';
export default class Checkout implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    action([values]: [string[]], { interactiveMerge, ours, theirs, manual, reset, all, verbose, skipNpmInstall, ignorePackageJson, conf, ignoreDist }: {
        interactiveMerge?: boolean;
        ours?: boolean;
        theirs?: boolean;
        manual?: boolean;
        reset?: boolean;
        all?: boolean;
        verbose?: boolean;
        skipNpmInstall?: boolean;
        ignorePackageJson?: boolean;
        conf?: string;
        ignoreDist?: boolean;
    }): Promise<ApplyVersionResults>;
    report({ components, version, failedComponents }: ApplyVersionResults): string;
}
