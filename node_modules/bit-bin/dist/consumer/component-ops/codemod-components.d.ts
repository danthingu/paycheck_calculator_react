import { BitId } from '../../bit-id';
import { Consumer } from '..';
export declare type CodemodResult = {
    id: BitId;
    changedFiles: string[];
    warnings?: string[];
};
export declare function changeCodeFromRelativeToModulePaths(consumer: Consumer, bitIds: BitId[]): Promise<CodemodResult[]>;
