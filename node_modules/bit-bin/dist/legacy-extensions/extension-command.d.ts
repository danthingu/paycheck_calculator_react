import { LegacyCommand, CommandOptions } from '../cli/legacy-command';
declare type ExtensionCommandProps = {
    name: string;
    action: Function;
    report: Function;
    description: string;
    opts?: CommandOptions;
};
export default class ExtensionCommand implements LegacyCommand {
    name: string;
    description: string;
    alias: string;
    opts: CommandOptions;
    private: boolean;
    migration: boolean;
    extension: boolean;
    loader: boolean;
    _action: any;
    _report: any;
    constructor(props: ExtensionCommandProps);
    action(relevantArgs: any, opts: any): Promise<any>;
    report(data: any): string;
}
export {};
