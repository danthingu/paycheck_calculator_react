import { BitId } from '../bit-id';
export default class BitIdAndValueArray<T> extends Array<{
    id: BitId;
    value: T;
}> {
    getValue(id: BitId): T | null;
    getValueIgnoreVersion(id: BitId): T | null;
    getValueIgnoreScopeAndVersion(id: BitId): T | null;
}
