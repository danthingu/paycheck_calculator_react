import { BaseCompiler } from '../base-compiler';
declare class Mock {
}
export declare class Babel {
    private mock?;
    constructor(mock?: Mock | undefined);
    provide([baseCompiler]: [BaseCompiler]): Promise<Babel>;
}
export {};
