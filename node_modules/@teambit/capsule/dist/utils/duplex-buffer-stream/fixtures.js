"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const duplex_buffer_stream_1 = require("./duplex-buffer-stream");
const simpleString = 'This is a String!';
const unicodeString = '\u00bd + \u00bc = \u00be';
const binaryData = new Buffer(64);
for (var i = 0; i < binaryData.length; i++) {
    binaryData[i] = i;
}
// Binary data larger than initial size of buffers.
const largeBinaryData = new Buffer(duplex_buffer_stream_1.DEFAULT_INITIAL_SIZE + 1);
for (i = 0; i < largeBinaryData.length; i++) {
    largeBinaryData[i] = i % 256;
}
exports.default = {
    simpleString,
    unicodeString,
    binaryData,
    largeBinaryData
};
//# sourceMappingURL=fixtures.js.map