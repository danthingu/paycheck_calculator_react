import { AnyFS } from '@teambit/any-fs';
import Container from './container';
import { Exec } from './exec';

export type ContainerFactoryOptions = {
  wrkDir?: string;
};

/*export type Container = {
  name: string,
  status: string
};*/

export default interface ContainerFactory<T extends Exec, X extends AnyFS> {
  createContainer(options?: ContainerFactoryOptions): Promise<Container<T, X>>;

  obtain(config: any): Promise<Container<T, X>>;

  // list(): Promise<Container[]>;
}
